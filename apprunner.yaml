version: 1.0
runtime: python3.11.0
build:
  commands:
    build:
      build:
      - pip install torch==2.1.1+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html
      - pip install --no-cache-dir -r requirements.txt
run:
  command: python server.py