version: 1.0
runtime: python3.11.0
build:
  commands:
    build:
      build:        
      - pip install pipenv
      - pipenv install
      - pip install -r requirements.txt
run:
  command: python server.py